//Question 87:
// Extracting a Substring: Demonstrate how to extract the first 10 characters from a string.

function extractingSubstring(char: string): string {
  return char.substring(0, 10);
}

console.log(extractingSubstring("I'm investing myself in Typescript"));
